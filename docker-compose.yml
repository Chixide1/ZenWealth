name: zenwealth
services:
  zenwealth-api:
    container_name: api
    build:
      context: .
      dockerfile: ZenWealth.Api/Dockerfile

  zenwealth-web:
    container_name: web
    build:
      context: .
      dockerfile: ZenWealth.Web/Dockerfile

