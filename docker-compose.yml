name: zenwealth
services:
  zenwealth.api:
    container_name: api
    image: ${DOCKER_REGISTRY-}zenwealth-api
    build:
      context: .
      dockerfile: ZenWealth.Api/Dockerfile

  zenwealth.web:
    container_name: web
    image: ${DOCKER_REGISTRY-}zenwealth-web
    build:
      context: .
      dockerfile: ZenWealth.Web/Dockerfile

