name: zenwealth
services:
  zenwealth-web:
    container_name: web
    build:
      context: .
      dockerfile: ZenWealth.Web/Dockerfile

    depends_on:
      - zenwealth-api

  zenwealth-api:
    container_name: api
    build:
      context: .
      dockerfile: ZenWealth.Api/Dockerfile

    depends_on:
      zenwealth-db:
        condition: service_healthy

  zenwealth-db:
    container_name: db
    image: postgres:18
    entrypoint: /docker-entrypoint.sh
    environment:
      POSTGRES_USER: zenwealth_app
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: zenwealth
    volumes:
      - ./ZenWealth.Infrastructure/Persistence/Migrations/PostgreSql/entrypoint.sh:/docker-entrypoint.sh
      - ./ZenWealth.Infrastructure/Persistence/Migrations/PostgreSql/script.sql:/docker-entrypoint-initdb.d/01-init.sql
      - pgdata:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U zenwealth_app -d zenwealth"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata: